// Phase 1 — placeholder. Composants réels (moteurs par défaut, budget,
// status clés API) à implémenter en Phase 5.
export default function SettingsPage() {
  return (
    <section>
      <h2>Settings</h2>
      <p>Moteur 3D / image / template par défaut, budget quotidien, clés API.</p>
    </section>
  )
}
